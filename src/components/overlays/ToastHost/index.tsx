import React from 'react';

/**
 * ToastHost Component
 * 
 * Global toast notification'ları render eden host component.
 * GlobalUIHost içinde kullanılır.
 * 
 * TODO: Toast management system implementation
 * - NotificationToast component'i zaten var
 * - Global toast state management eklenebilir
 */
export const ToastHost: React.FC = () => {
  // Future: Toast management system
  // NotificationToast component'i şu anda NotificationProvider içinde kullanılıyor
  // Global toast host için ayrı bir state management gerekebilir
  return null;
};
